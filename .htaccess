<IfModule mod_rewrite.h>
    RewriteEngine On
    
    # منع تصفح المجلدات
    Options -Indexes

    # توجيه جميع الطلبات إلى مجلد public وملف index.php داخله
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/index.php?url=$1 [QSA,L]
</IfModule>
