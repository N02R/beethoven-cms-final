<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # إذا كان الطلب يشير إلى ملف أو مجلد موجود، لا تفعلي شيئاً
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # أي طلب آخر، وجهيه إلى index.php
    RewriteRule ^ index.php [QSA,L]
</IfModule>
